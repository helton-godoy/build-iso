[{"node:child_process": "Get the project ID based on git root commit hash\n * Falls back to directory hash if not in a git repository\n */\nexport async function getProjectId(directory: string): Promise<string> {\n\ttry {\n\t\t// Try to get the git root commit hash\n\t\tconst gitRoot = execSync(", "show-toplevel": {"cwd": "directory", "encoding": "utf-8", "stdio": ["pipe", "pipe", "ignore"]}, "HEAD": {"cwd": "gitRoot", "encoding": "utf-8", "stdio": ["pipe", "pipe", "ignore"]}, "Fallback": "use directory name hash if not in git\n\t\tconst dirName = path.basename(directory)\n\t\treturn simpleHash(dirName)"}, {"crypto.createHash(\"sha256\").update(str).digest(\"hex": "."}]